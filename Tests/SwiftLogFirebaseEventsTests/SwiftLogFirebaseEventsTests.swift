import XCTest
@testable import SwiftLogFirebaseEvents

final class SwiftLogFirebaseEventsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftLogFirebaseEvents().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
